# Minimal engine-required home used only during hidden game-start setup.
# Like Kuat's origin placeholder, its population and mobile fleets are moved to
# the dynamically spawned lane-isolated Ilum at the L-Cluster-style outer
# radius, then this temporary planet is removed.
fop_ilum_system = {
    name = random
    class = "sc_k"
    usage = origin
    flags = { empire_home_system fop_ilum_system }
    max_instances = 1

    init_effect = { generate_home_system_resources = yes }

    planet = {
        class = "pc_k_star"
        orbit_distance = 0
        orbit_angle = 1
        size = 20
        has_ring = no
    }

    planet = {
        class = "pc_arctic"
        orbit_distance = 55
        orbit_angle = 30
        size = 22
        starting_planet = yes
        home_planet = yes
        deposit_blockers = none
        modifiers = none

        init_effect = {
            prevent_anomaly = yes
            generate_empire_home_planet = yes
        }
    }
}

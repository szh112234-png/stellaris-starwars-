# FOC - register the mod's own component tags.
#
# error.log: "unknown component tag [...]. Register tags in common/component_tags/"
# Any tag used in a component_template's tags = { } / ai_tags = { } block must be
# declared here, otherwise the engine drops it and tag-based AI weighting and
# has_component_tag checks silently stop matching.
#
# One tag per line, no braces.

XL_slot

weapon_role_medium
weapon_role_point_defence

weapon_type_point_defence
weapon_type_kuat_artellarys_scale
weapon_type_kuat_strikcrafe_scale

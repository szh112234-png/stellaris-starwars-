# First Order origin start screen. The origin carries custom_start_screen, so
# the generic prehistoric evolution and first-hyperdrive paragraphs are suppressed.
part = {
    location = 0
    localization = "START_SCREEN_FOP_FIRST_ORDER"

    trigger = {
        has_origin = origin_fop_first_order
    }
}

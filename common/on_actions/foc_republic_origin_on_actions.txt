on_game_start_country = {
    events = { foc_republic_origin.1 }
}

on_monthly_pulse_country = {
    events = {
        # Core chronology and project progression.
        foc_republic_origin.205
        foc_republic_origin.22

        # Clone War / civil-war / Treaty story progression.
        foc_republic_origin.19
        foc_republic_origin.202
        foc_republic_origin.29
        foc_republic_origin.30
        # V231: post-Mustafar Vader keeper. Guarded by
        # foc_republic_origin_vader_crowned, so it never creates a second Vader.
        foc_republic_origin.241
        foc_republic_origin.40
        foc_republic_origin.140
        foc_republic_origin.184
        foc_republic_origin.141
        # V203: the event itself describes this as the Treaty Leia/D'Qar monthly
        # fallback; register it so a missed direct +23y dispatch can converge.
        foc_republic_origin.199
        # V204: the ownership-lock event already existed but was never scheduled.
        # It restores D'Qar only after a false New Republic scripted reclaim and
        # never overrides a genuine First Order conquest.
        foc_v161_dqar.1

        # First Order Plan and completed-research dispatchers.
        foc_republic_origin.162
        foc_republic_origin.163
        # .164 (Ilum/Jakku setup) is called directly by project completion.
        foc_republic_origin.179
        foc_republic_origin.168
    }
}

# The Separatist war is no longer tied to elections. It starts only after the
# pre-war Separatist Crisis reaches the Geonosis opening story event.

# V78: Akiva no longer ends the Clone War. The remaining occupation hooks are
# unrelated later-story handlers; the six-system surrender condition is checked
# by foc_republic_origin.19 on the monthly country pulse.
on_planet_occupied = {
    events = { foc_republic_origin.166 }
}

on_planet_conquer = {
    events = { foc_republic_origin.166 }
}

on_colony_conquer = {
    events = { foc_republic_origin.166 }
}

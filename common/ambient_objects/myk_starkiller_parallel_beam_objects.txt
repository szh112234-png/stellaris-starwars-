# Fixed-world-orientation target objects for planet strikes.
# No entity_face_object/base_angle_towards is used when they are created,
# so every planet beam shares the same system-space direction.
ambient_object = {
    name = "myk_starkiller_parallel_target_beam_object"
    entity = "myk_starkiller_parallel_target_impact_entity"
    selectable = no
    show_name = no
}

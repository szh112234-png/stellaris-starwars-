# FOC V143 - unlimited ship construction counts.
# Stellaris 4.4 ownership_limits are additive when extra files add another
# "default" block, so this file intentionally replaces the vanilla
# 00_ownership_limits.txt. Only the Death Star retains a country ship-count cap.
default = {
	ship_of_size_limits = {
		foc_deathstar_ships_limit
	}
}

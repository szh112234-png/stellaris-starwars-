namespace = foc_khan_207

# 2.08 compatibility stub. The direct Synth Queen minister migration was removed
# because it is not safe for a non-Cetana country and caused a crash.
event = {
	id = foc_khan_207.1
	hide_window = yes
	is_triggered_only = yes
	trigger = { always = no }
}

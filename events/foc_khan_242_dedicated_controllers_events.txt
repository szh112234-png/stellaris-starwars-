namespace = foc_khan_242

# 2.43 compatibility stubs. The 2.42 controller depended on is_fleet_idle and
# could never take control of fleets born with guard/hold orders.
event = {
    id = foc_khan_242.1
    hide_window = yes
    is_triggered_only = yes
}

event = {
    id = foc_khan_242.2
    hide_window = yes
    is_triggered_only = yes
}

namespace = foc_khan_238

# 2.40 stability stubs.
# The 2.38 transport controller was parsed incorrectly and caused
# foc_khan_238.12 to become an automatic daily popup. Keep all serialized IDs
# as silent triggered-only events so old saves can resolve queued events safely.
event = {
    id = foc_khan_238.10
    hide_window = yes
    is_triggered_only = yes
}

fleet_event = {
    id = foc_khan_238.11
    hide_window = yes
    is_triggered_only = yes
}

planet_event = {
    id = foc_khan_238.12
    hide_window = yes
    is_triggered_only = yes
}

event = {
    id = foc_khan_238.20
    hide_window = yes
    is_triggered_only = yes
}

namespace = foc_khan_243

# 2.45 compatibility stubs. The 2.43 combined controller was malformed and is
# no longer used. Old queued events resolve silently without taking fleet orders.
event = {
    id = foc_khan_243.1
    hide_window = yes
    is_triggered_only = yes
    trigger = { always = no }
}

event = {
    id = foc_khan_243.2
    hide_window = yes
    is_triggered_only = yes
    trigger = { always = no }
}

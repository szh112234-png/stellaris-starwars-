namespace = foc_khan_phase2fix

# Compatibility stubs retained for old saves. These IDs remain defined so old
# scheduled events expire without changing country type or fleet orders.
event = {
	id = foc_khan_phase2fix.1
	hide_window = yes
	is_triggered_only = yes
	trigger = { always = no }
}

country_event = {
	id = foc_khan_phase2fix.2
	hide_window = yes
	is_triggered_only = yes
	trigger = { always = no }
}

country_event = {
	id = foc_khan_phase2fix.3
	hide_window = yes
	is_triggered_only = yes
	trigger = { always = no }
}

namespace = foc_swgs_fix

country_event = {
    id = foc_swgs_fix.1
    hide_window = yes
    is_triggered_only = yes

    trigger = {
        OR = {
            has_country_flag = foc_khan_first_order_country
            has_country_flag = foc_khan_final_order_country
            has_country_flag = foc_khan_rebel_alliance_country
            has_country_flag = foc_republic_origin_empire_country
            has_country_flag = foc_sw_imperial_remnant_country
        }
        NOT = {
            has_technology = foc_tech_swgs_crisis_point_defence
        }
    }

    immediate = {
        if = { limit = { is_ai = yes } give_technology = {
            tech = foc_tech_swgs_crisis_point_defence
            message = no
        } }
    }
}

country_event = {
    id = foc_swgs_fix.2
    hide_window = yes
    is_triggered_only = yes
    trigger = { always = no }
}

namespace = foc_khan_237

# 2.41 compatibility stub. Old saves may still have this ID queued.
event = {
    id = foc_khan_237.1
    hide_window = yes
    is_triggered_only = yes
    immediate = { set_global_flag = foc_khan_237_acot_full_port_migrated }
}

namespace = foc_khan_sgk_ai

# 2.20: retired. Ordinary First/Final Order fleets now use the same engine
# berserk military minister pattern as ACOT. IDs are retained so old saves with
# scheduled events resolve safely without reinstalling persistent queues.
event = {
    id = foc_khan_sgk_ai.1
    hide_window = yes
    is_triggered_only = yes
    trigger = { always = no }
}

fleet_event = {
    id = foc_khan_sgk_ai.10
    hide_window = yes
    is_triggered_only = yes
    trigger = { always = no }
}

fleet_event = {
    id = foc_khan_sgk_ai.20
    hide_window = yes
    is_triggered_only = yes
    trigger = { always = no }
}

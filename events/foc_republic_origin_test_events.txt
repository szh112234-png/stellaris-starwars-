namespace = foc_republic_origin_command

# 最高统帅部作战推演台。每个选项都只在对应剧情阶段真正存在时才显示，因此
# 反复使用不会重复生成国家、战斗空间站或条约。V232s将强制投降明确标为调试，
# 与正常胜利条件的只读检查分开，强制命令不再受剧情门槛限制。
country_event = {
    id = foc_republic_origin_command.1
    title = foc_republic_origin_command.1.name
    desc = foc_republic_origin_command.1.desc
    picture = GFX_foc_event_war_in_heaven
    is_triggered_only = yes
    force_open = yes

    # V183: debug controls must follow the player's selected civil-war faction.
    # Reacquire the active Empire/Rebel global targets before option triggers are
    # evaluated so manual controls never disappear because a route transformation
    # cleared an old event target.
    immediate = {
        if = {
            limit = { has_country_flag = foc_republic_origin_empire_country }
            save_global_event_target_as = foc_republic_origin_empire
            save_global_event_target_as = foc_sw_imperial_remnant
        }
        else_if = {
            limit = {
                NOT = { exists = event_target:foc_republic_origin_empire }
                any_country = { has_country_flag = foc_republic_origin_empire_country }
            }
            random_country = {
                limit = { has_country_flag = foc_republic_origin_empire_country }
                save_global_event_target_as = foc_republic_origin_empire
                save_global_event_target_as = foc_sw_imperial_remnant
            }
        }
        if = {
            limit = {
                OR = {
                    has_country_flag = foc_republic_origin_imperial_rebel_country
                    has_country_flag = foc_republic_origin_player_rebels
                    has_country_flag = foc_khan_rebel_alliance_country
                }
            }
            save_global_event_target_as = foc_khan_rebel_alliance
            save_global_event_target_as = foc_republic_origin_imperial_rebels
        }
        else_if = {
            limit = {
                NOT = { exists = event_target:foc_khan_rebel_alliance }
                any_country = {
                    OR = {
                        has_country_flag = foc_republic_origin_imperial_rebel_country
                        has_country_flag = foc_khan_rebel_alliance_country
                    }
                }
            }
            random_country = {
                limit = {
                    OR = {
                        has_country_flag = foc_republic_origin_imperial_rebel_country
                        has_country_flag = foc_khan_rebel_alliance_country
                    }
                }
                save_global_event_target_as = foc_khan_rebel_alliance
                save_global_event_target_as = foc_republic_origin_imperial_rebels
            }
        }
    }

    # V97 universal chronology/debug controls. These are explicit manual bypasses
    # only; normal gameplay remains governed by the selected 2200/2250/2300/2400 gate.
    option = {
        name = foc_republic_origin_command.1.release_timeline
        trigger = {
            is_ai = no
            NOT = { foc_story_timeline_start_due = yes }
        }
        hidden_effect = {
            remove_global_flag = foc_story_timeline_start_2250
            remove_global_flag = foc_story_timeline_start_2300
            remove_global_flag = foc_story_timeline_start_2400
            set_global_flag = foc_story_timeline_start_2200

            remove_global_flag = foc_story_timeline_naboo_immediate
            remove_global_flag = foc_story_timeline_naboo_2150
            remove_global_flag = foc_story_timeline_naboo_2250
            remove_global_flag = foc_story_timeline_naboo_2300
            remove_global_flag = foc_story_timeline_naboo_2400
            set_global_flag = foc_story_timeline_naboo_2200

            # Dispatch the correct route immediately instead of waiting for a monthly pulse.
            if = {
                limit = {
                    has_country_flag = foc_republic_origin_republic_country
                    has_country_flag = foc_republic_origin_naboo_timeline_pending
                }
                country_event = { id = foc_republic_origin.205 }
            }
            if = {
                limit = {
                    has_origin = origin_fop_first_order
                    has_country_flag = fop_origin_relocated_to_ilum
                    has_country_flag = fop_origin_story_timeline_locked
                    NOT = { foc_khan_owns_completed_starkiller = yes }
                }
                country_event = { id = fop_origin.205 }
            }
        }
    }

    option = {
        name = foc_republic_origin_command.1.fo_origin_repair
        trigger = {
            is_ai = no
            has_origin = origin_fop_first_order
            foc_khan_owns_completed_starkiller = yes
            NOT = { has_country_flag = fop_origin_crisis_active }
        }
        hidden_effect = {
            # V96 rule: once Starkiller is complete, the original FO-origin post-build
            # chain must never remain blocked by the shared chronology selector.
            remove_country_flag = fop_origin_story_timeline_locked
            set_country_flag = fop_origin_story_timeline_released
            country_event = { id = fop_origin.20 }
        }
    }



    option = {
        name = foc_republic_origin_command.1.start_separatists
        trigger = {
            has_country_flag = foc_republic_origin_initialized
            has_country_flag = foc_republic_origin_republic_country
            NOT = { has_country_flag = foc_republic_origin_separatist_war_started }
        }
        hidden_effect = {
            # 作战推演必须能直接跳到分离主义战争。正常剧情的 .10 现在有一整套
            # 纳布危机→杜库→克隆军→卡米诺→吉奥诺西斯前置门槛；如果这里只是
            # 直接调用 .10，事件会因为 trigger 不满足而静默失败。
            remove_country_flag = foc_republic_origin_initial_reform_grace
            set_country_flag = foc_republic_origin_separatist_situation_complete
            set_country_flag = foc_republic_origin_separatist_chain_complete
            set_country_flag = foc_sepplot_naboo_registered
            set_country_flag = foc_sepplot_sith_apprentice_seen
            set_country_flag = foc_sepplot_sifo_dyas_ordered
            set_country_flag = foc_sepplot_dooku_hijacked_clone_plan
            set_country_flag = foc_sepplot_separatist_movement_declared
            set_country_flag = foc_sepplot_corporate_alliance_formed
            set_country_flag = foc_sepplot_kamino_discovered
            set_country_flag = foc_sepplot_geonosis_plot_discovered
            set_country_flag = foc_sepplot_jedi_captured
            set_country_flag = foc_sepplot_emergency_powers_granted
            set_country_flag = foc_sepplot_clone_army_activation_done
            set_country_flag = foc_saga_cw_geonosis_shown
            set_global_flag = foc_saga_cw_begun
            country_event = { id = foc_republic_origin.10 }
        }
    }

    option = {
        name = foc_republic_origin_command.1.surrender_separatists
        trigger = {
            is_ai = no
            foc_v197_republic_story_actor = yes
            has_country_flag = foc_republic_origin_republic_country
            NOT = { has_country_flag = foc_republic_origin_separatist_war_won }
            NOT = { has_country_flag = foc_republic_origin_separatist_surrender_staging_142 }
            NOT = { has_country_flag = foc_republic_origin_order_66_executed }
            OR = {
                has_global_flag = foc_republic_origin_separatist_war_active
                any_country = { has_country_flag = foc_republic_origin_separatist_country }
            }
        }
        custom_tooltip = foc_v232s.debug_surrender_tooltip
        hidden_effect = {
            # Explicit debug bypass: no .19/.20 and no forged battle/score flags.
            foc_v232s_commit_separatist_surrender = yes
            log = "FOC_V232S_DEBUG: explicit force-surrender command; normal victory gates bypassed; staged cleanup requested."
        }
    }

    option = {
        name = foc_v232s.debug_status_button
        trigger = { is_ai = no has_country_flag = foc_republic_origin_republic_country }
        hidden_effect = { country_event = { id = foc_v232s_cis.1 } }
    }

    option = {
        name = foc_republic_origin_command.1.plot_25
        trigger = {
            has_country_flag = foc_republic_origin_palpatine_plot_active
            any_situation = {
                is_situation_type = situation_foc_republic_origin_palpatine_plot
            }
        }
        hidden_effect = { foc_v232r_recalculate_plot = yes }
    }

    option = {
        name = foc_republic_origin_command.1.plot_complete
        trigger = {
            has_country_flag = foc_republic_origin_palpatine_plot_active
            any_situation = {
                is_situation_type = situation_foc_republic_origin_palpatine_plot
            }
        
            has_country_flag = foc_republic_origin_separatist_war_won
        }
        hidden_effect = {
            random_situation = {
                limit = {
                    is_situation_type = situation_foc_republic_origin_palpatine_plot
                }
                add_situation_progress = 1000
            }
            # V151 command-panel fast path: when the Clone War has already ended,
            # finish the canonical Mandalore prerequisite instead of leaving Order 66
            # pending forever in rapid story simulation.
            if = {
                limit = {
                    has_country_flag = foc_republic_origin_separatist_war_won
                    NOT = { has_global_flag = foc_republic_origin_separatist_war_active }
                    NOT = { has_global_flag = foc_canon_mandalore_siege_completed }
                    NOT = { has_global_flag = foc_republic_origin_jedi_purged }
                }
                if = {
                    limit = { NOT = { has_country_flag = foc_canon_maul_mandalore } }
                    set_country_flag = foc_canon_maul_mandalore
                }
                country_event = { id = foc_canon.41 days = 1 }
            }
        }
    }

    option = {
        name = foc_republic_origin_command.1.build_death_star_one
        trigger = {
            exists = event_target:foc_republic_origin_empire
            event_target:foc_republic_origin_empire = {
                has_country_flag = foc_republic_origin_empire_country
                NOT = { has_country_flag = foc_republic_origin_death_star_one_built }
            }
        }
        hidden_effect = {
            event_target:foc_republic_origin_empire = {
                # V183 debug bypass: .170 is the real completion path and requires
                # the engineering-complete flag for a human Empire.
                log = "FOC_DEBUG_DS1_BUILD_REQUEST: debug menu requested immediate Death Star I commissioning; normal completion guards still apply."
                set_country_flag = foc_v138_death_star_one_engineering_work_complete
                country_event = { id = foc_republic_origin.170 }
            }
        }
    }

    option = {
        name = foc_republic_origin_command.1.destroy_death_star_one
        trigger = {
            exists = event_target:foc_republic_origin_empire
            event_target:foc_republic_origin_empire = {
                has_country_flag = foc_republic_origin_death_star_one_built
                NOT = { has_country_flag = foc_republic_origin_death_star_one_destroyed }
            }
        }
        hidden_effect = {
            event_target:foc_republic_origin_empire = {
                log = "FOC_DEBUG_DS1_DESTROY: debug menu explicitly destroys Death Star I with destroy_ship; the explosion is intentional."
                every_owned_ship = {
                    limit = { has_ship_flag = foc_republic_origin_death_star_one }
                    destroy_ship = this
                }
                set_country_flag = foc_republic_origin_death_star_one_destroyed
            }
            set_global_flag = foc_republic_origin_death_star_one_destroyed
            country_event = { id = foc_republic_origin.171 }
        }
    }

    option = {
        name = foc_republic_origin_command.1.build_death_star_two
        trigger = {
            exists = event_target:foc_republic_origin_empire
            NOT = { has_global_flag = foc_republic_origin_death_star_two_destroyed }
            event_target:foc_republic_origin_empire = {
                has_country_flag = foc_republic_origin_empire_country
                has_country_flag = foc_republic_origin_death_star_one_destroyed
                NOT = { has_country_flag = foc_republic_origin_death_star_two_built }
                NOT = { has_country_flag = foc_republic_origin_death_star_two_destroyed }
            }
        }
        hidden_effect = {
            # Recheck when clicked: another open debug menu can become stale.
            if = {
                limit = {
                    exists = event_target:foc_republic_origin_empire
                    NOT = { has_global_flag = foc_republic_origin_death_star_two_destroyed }
                    event_target:foc_republic_origin_empire = {
                        has_country_flag = foc_republic_origin_empire_country
                        has_country_flag = foc_republic_origin_death_star_one_destroyed
                        NOT = { has_country_flag = foc_republic_origin_death_star_two_built }
                        NOT = { has_country_flag = foc_republic_origin_death_star_two_destroyed }
                    }
                }
                event_target:foc_republic_origin_empire = {
                    log = "FOC_DEBUG_DS2_BUILD_REQUEST: debug menu skips engineering construction and the AI four-year Bothan wait, then calls the guarded DS2 completion event."
                    set_country_flag = foc_ds2_program_started
                    set_country_flag = foc_v138_death_star_two_engineering_work_complete
                    remove_country_flag = foc_ds2_bothan_wait
                    if = {
                        limit = { is_ai = yes }
                        foc_ds2_reveal_bothan_intelligence = yes
                    }
                    country_event = { id = foc_republic_origin.174 }
                }
            }
        }
    }

    option = {
        name = foc_republic_origin_command.1.destroy_death_star_two
        trigger = {
            exists = event_target:foc_republic_origin_empire
            event_target:foc_republic_origin_empire = {
                has_country_flag = foc_republic_origin_death_star_two_built
                NOT = { has_country_flag = foc_republic_origin_death_star_two_destroyed }
            }
        }
        hidden_effect = {
            event_target:foc_republic_origin_empire = {
                log = "FOC_DEBUG_DS2_DESTROY: debug menu explicitly destroys Death Star II with destroy_ship; the explosion is intentional."
                every_owned_ship = {
                    limit = { has_ship_flag = foc_republic_origin_death_star_two }
                    destroy_ship = this
                }
                set_country_flag = foc_republic_origin_death_star_two_destroyed
            }
            set_global_flag = foc_republic_origin_death_star_two_destroyed
            country_event = { id = foc_republic_origin.175 }
            if = {
                limit = {
                    has_global_flag = foc_republic_origin_death_star_one_destroyed
                    NOT = { has_global_flag = foc_republic_origin_both_death_stars_destroyed }
                }
                country_event = { id = foc_republic_origin.176 }
            }
        }
    }

    option = {
        name = foc_republic_origin_command.1.force_treaty
        trigger = {
            has_global_flag = foc_republic_origin_empire_war_active
            NOT = { has_global_flag = foc_republic_origin_galactic_treaty }
            exists = event_target:foc_republic_origin_empire
            exists = event_target:foc_khan_rebel_alliance
            OR = {
                has_country_flag = foc_republic_origin_player_empire
                has_country_flag = foc_republic_origin_player_rebels
            }
        }
        hidden_effect = {
            # Debug means an unconditional story bypass: establish every
            # military gate directly before dispatching the side-aware Treaty.
            # This deliberately does not require physical ownership of the
            # three objective systems.
            set_global_flag = foc_republic_origin_death_star_one_destroyed
            set_global_flag = foc_republic_origin_death_star_two_destroyed
            set_global_flag = foc_republic_origin_both_death_stars_destroyed
            set_global_flag = foc_republic_origin_executor_one_destroyed
            set_global_flag = foc_republic_origin_executor_two_destroyed
            set_global_flag = foc_republic_origin_executor_three_destroyed
            set_global_flag = foc_republic_origin_all_executors_destroyed
            set_global_flag = foc_republic_origin_both_executors_destroyed
            set_global_flag = foc_republic_origin_treaty_military_conditions_met
            event_target:foc_republic_origin_empire = {
                set_country_flag = foc_republic_origin_death_star_one_destroyed
                set_country_flag = foc_republic_origin_death_star_two_destroyed
                set_country_flag = foc_republic_origin_executor_one_commissioned
                set_country_flag = foc_republic_origin_executor_two_commissioned
                set_country_flag = foc_republic_origin_executor_three_commissioned
                set_country_flag = foc_republic_origin_executor_one_destroyed
                set_country_flag = foc_republic_origin_executor_two_destroyed
                set_country_flag = foc_republic_origin_executor_three_destroyed
            }
            if = {
                limit = {
                    NOT = {
                        has_global_flag = foc_republic_origin_both_death_stars_destroyed
                    }
                }
                country_event = { id = foc_republic_origin.176 }
            }
            if = {
                limit = { has_country_flag = foc_republic_origin_player_rebels }
                country_event = { id = foc_republic_origin.160 days = 1 }
            }
            else_if = {
                limit = { has_country_flag = foc_republic_origin_player_empire }
                country_event = { id = foc_republic_origin.161 days = 1 }
            }
        }
    }

    option = {
        name = foc_republic_origin_command.1.grant_mon_cala
        trigger = {
            # 不再要求条约旗标：那个旗标本身就可能是缺失的一环，拿它当前置
            # 会把唯一的手动出口也一起关掉。
            always = yes
        }
        hidden_effect = {
            country_event = { id = foc_nr_fix.3 }
        }
    }

    option = {
        name = foc_republic_origin_command.1.close
        country_event = { id = foc_khan_settings.10 }
    }
}
